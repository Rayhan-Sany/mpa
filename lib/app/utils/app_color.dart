import 'package:flutter/material.dart';

class AppColor {
  static const primaryColor = Color(0xFF7255FD);
  static const textColor = Colors.white70;
  static const successColor = Colors.green;
  static const errorColor = Colors.red;
}
